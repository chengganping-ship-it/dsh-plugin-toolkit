# dsh-tool-promptlab

Prompt Engineering Lab plugin for DeepSeek Harness (DSH).

## Overview

A comprehensive prompt engineering toolkit that helps optimize, evaluate, and manage prompts to achieve up to 85% code usability from accurate prompts.

## Tools

| # | Tool | Description |
|---|------|-------------|
| 1 | `prompt_optimizer` | Optimize raw prompts for clarity and effectiveness |
| 2 | `template_library` | Browse and customize prompt templates by domain |
| 3 | `quality_scorer` | Score prompt quality with actionable feedback |
| 4 | `ab_test_designer` | Design A/B tests for prompt variants |
| 5 | `context_engineer` | Optimize context selection within token budgets |
| 6 | `chain_builder` | Design multi-step prompt chains |
| 7 | `output_validator` | Validate outputs against expected formats |
| 8 | `prompt_analyzer` | Deep analysis of prompt structure and tokens |

## Installation

```bash
npm install
```

## Build

```bash
npx tsc
```

## Usage

```typescript
import { tools } from 'dsh-tool-promptlab';

// Optimize a prompt
const result = tools.prompt_optimizer({
  raw_prompt: 'Write code for sorting',
  target_task: 'Generate a quicksort implementation',
  constraints: ['O(n log n) time complexity', 'In-place sorting']
});

// Score prompt quality
const score = tools.quality_scorer({
  prompt_text: 'You are an expert developer. Write a Python function...',
  evaluation_criteria: ['Clarity', 'Specificity', 'Completeness']
});
```

## Architecture

```
dsh-tool-promptlab/
  package.json
  tsconfig.json
  cordis.yml
  src/
    index.ts        # All 8 tools + interfaces + utilities
  lib/              # Compiled output (generated by tsc)
```

## License

MIT
