# DeepSeek Harness Plugin: LLM Token Compression & Optimization Engine v0.1.0
# Prompt compression, context window management, cost optimization

- id: dsh-tool-compress
  name: dsh-tool-compress
  version: 0.1.0
  description: LLM Token Compression & Optimization Engine - Prompt compression, context window optimizer, token budget planner, semantic deduplicator, summarization pipeline, embedding cache advisor, batch optimizer, cost analyzer
  author: chengganping-ship-it
  tools:
    - prompt_compressor
    - context_window_optimizer
    - token_budget_planner
    - semantic_deduplicator
    - summarization_pipeline
    - embedding_cache_advisor
    - batch_optimizer
    - cost_analyzer
